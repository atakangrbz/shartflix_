name: shartflix_
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.1
  flutter_bloc: ^8.1.2
  equatable: ^2.0.5
  http: ^1.4.0  # ✅ http sürümü yükseltildi (uyumlu)
  font_awesome_flutter: ^10.7.0
  shared_preferences: ^2.0.15
  flutter_svg: ^2.0.10  # ✅ bu sürümle uyumlu hale geldi

  # 🔥 Firebase paketleri:
  firebase_core: ^2.30.0
  firebase_auth: ^4.17.8
  cloud_firestore: ^4.15.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
